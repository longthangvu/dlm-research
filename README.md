# dlm-research

## Benchmark Workflow

Use the benchmark scripts in `benchmarks/` to compare Qwen3-1.7B and Fast-dLLM v2
under increasing context length, run LongBench quality gates, and measure
task-shaped performance.

See `benchmarks/README.md` for commands and outputs.
